This is a load balancer with simulated requests to model how a cloud service provider would use load-balancing to distribute web requests among their servers.

use these commands to compile and execute:

g++ \*.cpp -o loadbalancer

./loadbalancer
